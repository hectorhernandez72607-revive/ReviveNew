# ==============================================================================
# ENVIRONMENT VARIABLES FOR LEAD DASHBOARD
# ==============================================================================
# 
# 1. Copy this file to .env:
#    cp .env.example .env
#
# 2. Fill in your values below
#
# 3. Never commit .env to git (it's in .gitignore)
# ==============================================================================

# Resend API Key
# Get yours at: https://resend.com/api-keys
RESEND_API_KEY=re_your_api_key_here

# Sender Email
# For testing, use: onboarding@resend.dev
# For production, verify your domain in Resend and use your email
SENDER_EMAIL=onboarding@resend.dev

# Sender Name (appears in the "From" field)
SENDER_NAME=Your Business Name

# JWT secret for auth (signup / login). MUST be strong in production!
# Generate: openssl rand -hex 32
JWT_SECRET=your-random-secret-here

# ------------------------------------------------------------------------------
# Security (production hardening)
# ------------------------------------------------------------------------------
# Set to true ONLY for local dev. Leave false or unset in production.
# ENABLE_TEST_ENDPOINTS=false

# When set, test endpoints require header: X-Admin-Key: <ADMIN_API_KEY>
# Generate: openssl rand -hex 24
# ADMIN_API_KEY=

# Restrict CORS to your frontend origin(s). Comma-separated. * = allow all.
# CORS_ORIGINS=https://app.yourdomain.com,https://yourdomain.com

# ------------------------------------------------------------------------------
# Email lead ingestion (Gmail IMAP) — no domain required
# ------------------------------------------------------------------------------
# Use the Gmail address that receives lead emails. Create an App Password:
# Google Account → Security → App passwords → Mail → Generate. Use that 16-char
# password below, not your normal Gmail password.
IMAP_EMAIL=your_email@gmail.com
IMAP_APP_PASSWORD=your_16_char_app_password

# Client slug for ingested leads (same as in dashboard "Connect your Google Form")
IMAP_CLIENT_SLUG=demo

# Optional: IMAP connect/login timeout in seconds (default 15). Use if you hit timeouts.
# IMAP_TIMEOUT=15

# ------------------------------------------------------------------------------
# SMS / Messages (Twilio) — leads from texts + SMS autofollowups
# ------------------------------------------------------------------------------
# Get credentials: https://console.twilio.com
# TWILIO_ACCOUNT_SID=AC...
# TWILIO_AUTH_TOKEN=...
# TWILIO_PHONE_NUMBER=+15551234567
# Client slug for leads that text your Twilio number (defaults to IMAP_CLIENT_SLUG if unset)
# TWILIO_CLIENT_SLUG=demo

# ------------------------------------------------------------------------------
# AI "is this a lead?" classifier (optional)
# ------------------------------------------------------------------------------
# When set, only emails the model classifies as potential leads are added.
# Get an API key: https://platform.openai.com/api-keys
# OPENAI_API_KEY=sk-your-key-here
# OPENAI_MODEL=gpt-4o-mini
